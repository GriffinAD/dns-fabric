"""Placeholder OpenAPI export until the FastAPI app is wired."""

from __future__ import annotations


def main() -> None:
    print(
        "export_openapi: bootstrap mode — OpenAPI is hand-maintained in "
        "specs/api/openapi.yaml. Replace this script when the HTTP app is wired."
    )


if __name__ == "__main__":
    main()
